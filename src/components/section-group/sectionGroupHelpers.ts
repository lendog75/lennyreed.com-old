export interface SectionGroupModel {
  heading?: string;
  text?: string;
  children?: any;
}

export const styles = {
  section: { px: 6, py: 3 },
  heading: { textAlign: "left" },
  content: { textAlign: "left" },
};
